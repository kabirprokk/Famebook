<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FameBook" parent="android:Theme.DeviceDefault.NoActionBar" />

    <!-- Translucent host for the outside-the-app incoming shoot popup -->
    <style name="Theme.FameBook.Incoming" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
